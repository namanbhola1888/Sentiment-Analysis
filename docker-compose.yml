version: '3.8'

services:
  emotion-analysis:
    build: .
    platform: linux/amd64
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - PYTHONUNBUFFERED=1

    restart: unless-stopped
